#include <stdio.h>

int main() {
    int score = 83;
    float gpa = 1.75;
    char grade = 'A';

    printf("A student scores %d in an exam.\nAn okay score for someone with gpa of %f and grade %c\n", score, gpa, grade);

    return 0;
}